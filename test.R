#Test 123

#changing on GitHub now :D

#running Shiny app directly from GitHub
runGitHub(repo = "myclass-repo",
          username = "davoodroshan",
          ref="main",
          subdir="App")

runGitHub(repo = "myclass",
          username = "oisin-ok",
          ref="main",
          subdir="App")
